"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { createFeeHeadAction, createFeeInstallmentAction, createFeeStructureAction } from "../actions/fee-configuration.actions";

type Option = { id: string; name: string };
type Head = Option & { code: string };
type Structure = { id: string; name: string; academicYearId: string; classId: string | null; effectiveFrom: Date; status: string };
type Installment = { id: string; name: string; feeStructureId: string; feeHeadId: string; amountMinor: number; dueOn: Date };

export function FeeConfigurationWorkspace({ heads, structures, installments, years, classes, canCreate }: { heads: Head[]; structures: Structure[]; installments: Installment[]; years: Option[]; classes: Option[]; canCreate: boolean }) {
  const router = useRouter(); const [message, setMessage] = useState("");
  async function submit(action: (value: Record<string, unknown>) => Promise<{ ok: boolean; message?: string; error?: string }>, form: HTMLFormElement) { const data = new FormData(form); const result = await action(Object.fromEntries(data.entries())); setMessage(result.ok ? result.message ?? "Saved." : result.error ?? "Unable to save."); if (result.ok) { form.reset(); router.refresh(); } }
  return <div className="space-y-6"><div className="grid gap-6 lg:grid-cols-3">{canCreate ? <Card><CardHeader><CardTitle>Fee head</CardTitle></CardHeader><CardContent><form className="space-y-3" onSubmit={(event) => { event.preventDefault(); void submit(createFeeHeadAction, event.currentTarget); }}><div><Label>Name</Label><Input name="name" required /></div><div><Label>Code</Label><Input name="code" required /></div><Button>Create head</Button></form></CardContent></Card> : null}{canCreate ? <Card><CardHeader><CardTitle>Fee structure</CardTitle></CardHeader><CardContent><form className="space-y-3" onSubmit={(event) => { event.preventDefault(); void submit(createFeeStructureAction, event.currentTarget); }}><div><Label>Academic year</Label><select className="h-10 w-full rounded-md border bg-background px-3 text-sm" name="academicYearId" required>{years.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><Label>Class (optional)</Label><select className="h-10 w-full rounded-md border bg-background px-3 text-sm" name="classId"><option value="">All classes</option>{classes.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><Label>Name</Label><Input name="name" required /></div><div><Label>Effective from</Label><Input name="effectiveFrom" type="date" required /></div><Button>Create structure</Button></form></CardContent></Card> : null}{canCreate ? <Card><CardHeader><CardTitle>Installment</CardTitle></CardHeader><CardContent><form className="space-y-3" onSubmit={(event) => { event.preventDefault(); void submit(createFeeInstallmentAction, event.currentTarget); }}><div><Label>Structure</Label><select className="h-10 w-full rounded-md border bg-background px-3 text-sm" name="feeStructureId" required>{structures.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div><Label>Fee head</Label><select className="h-10 w-full rounded-md border bg-background px-3 text-sm" name="feeHeadId" required>{heads.map((item) => <option key={item.id} value={item.id}>{item.name} ({item.code})</option>)}</select></div><div><Label>Name</Label><Input name="name" required /></div><div><Label>Amount (minor units)</Label><Input name="amountMinor" type="number" min="1" required /></div><div><Label>Due on</Label><Input name="dueOn" type="date" required /></div><Button>Create installment</Button></form></CardContent></Card> : null}</div>{message ? <p role="status" className="text-sm text-muted-foreground">{message}</p> : null}<Card><CardHeader><CardTitle>Fee heads and structures</CardTitle></CardHeader><CardContent><div className="grid gap-4 lg:grid-cols-2"><div><h3 className="mb-2 font-medium">Heads ({heads.length})</h3><ul className="space-y-2 text-sm">{heads.map((item) => <li className="rounded border p-2" key={item.id}>{item.name} <span className="text-muted-foreground">({item.code})</span></li>)}</ul></div><div><h3 className="mb-2 font-medium">Structures ({structures.length})</h3><ul className="space-y-2 text-sm">{structures.map((item) => <li className="rounded border p-2" key={item.id}>{item.name} <span className="text-muted-foreground">· {item.status} · effective {item.effectiveFrom.toLocaleDateString()}</span></li>)}</ul></div></div><h3 className="mb-2 mt-6 font-medium">Installments ({installments.length})</h3><ul className="space-y-2 text-sm">{installments.map((item) => <li className="rounded border p-2" key={item.id}>{item.name} · ₹{(item.amountMinor / 100).toLocaleString("en-IN", { minimumFractionDigits: 2 })} · due {item.dueOn.toLocaleDateString()}</li>)}</ul></CardContent></Card></div>;
}
