"use server";

import { revalidatePath } from "next/cache";
import { requirePermission } from "@/lib/auth/guards";
import { writeAuditLog } from "@/lib/audit/audit-log";
import type { ActionResult } from "@/lib/errors/result";
import { achievementSchema, alumniDonationSchema, alumniEventRegistrationSchema, alumniEventSchema, alumniProfileSchema, clubMembershipSchema, clubSchema, communityTransitionSchema, cmsFormSchema, cmsMediaSchema, cmsPageSchema, formSubmissionSchema, jobBoardPostSchema, mentorshipSchema, sportsFixtureSchema, sportsTeamSchema } from "../schemas/community.schema";
import { createAchievement, createAlumniDonation, createAlumniEvent, createAlumniEventRegistration, createAlumniProfile, createClub, createClubMembership, createCmsForm, createCmsMedia, createCmsPage, createFormSubmission, createJobBoardPost, createMentorship, createSportsFixture, createSportsTeam, transitionCmsForm, transitionCmsMedia, transitionCmsPage } from "../services/community.service";

function invalid(message: string): ActionResult<{ id: string }> { return { ok: false, error: message, code: "VALIDATION_ERROR" }; }

export async function createClubAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = clubSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Club details are invalid."); try { const user = await requirePermission("activities:create"); const row = await createClub(user, parsed.data); await writeAuditLog(user, { action: "create", module: "activities", entityType: "club", entityId: row.id, campusId: row.campusId, after: { name: row.name } }); revalidatePath("/activities/clubs"); return { ok: true, data: { id: row.id }, message: "Club created." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to create club." }; } }
export async function createAchievementAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = achievementSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Achievement details are invalid."); try { const user = await requirePermission("activities:create"); const row = await createAchievement(user, parsed.data); await writeAuditLog(user, { action: "create", module: "activities", entityType: "student_achievement", entityId: row.id, campusId: row.campusId, after: { studentId: row.studentId, title: row.title } }); revalidatePath("/activities/achievements"); revalidatePath("/students"); return { ok: true, data: { id: row.id }, message: "Achievement recorded." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to record achievement." }; } }
export async function createClubMembershipAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = clubMembershipSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Membership details are invalid."); try { const user = await requirePermission("activities:create"); const row = await createClubMembership(user, parsed.data); await writeAuditLog(user, { action: "create", module: "activities", entityType: "club_membership", entityId: row.id, campusId: row.campusId, after: { referenceId: row.referenceId } }); revalidatePath("/activities/clubs/memberships"); return { ok: true, data: { id: row.id }, message: "Student added to club." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to add club membership." }; } }
export async function createSportsTeamAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = sportsTeamSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Sports team details are invalid."); try { const user = await requirePermission("activities:create"); const row = await createSportsTeam(user, parsed.data); await writeAuditLog(user, { action: "create", module: "activities", entityType: "sports_team", entityId: row.id, campusId: row.campusId, after: { name: row.name } }); revalidatePath("/activities/sports"); return { ok: true, data: { id: row.id }, message: "Sports team created." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to create sports team." }; } }
export async function createSportsFixtureAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = sportsFixtureSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Fixture details are invalid."); try { const user = await requirePermission("activities:create"); const row = await createSportsFixture(user, parsed.data); await writeAuditLog(user, { action: "create", module: "activities", entityType: "sports_fixture", entityId: row.id, campusId: row.campusId, after: { name: row.name } }); revalidatePath("/activities/sports"); return { ok: true, data: { id: row.id }, message: "Sports fixture scheduled." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to schedule fixture." }; } }
export async function createAlumniProfileAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = alumniProfileSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Alumni profile details are invalid."); try { const user = await requirePermission("alumni:create"); const row = await createAlumniProfile(user, parsed.data); await writeAuditLog(user, { action: "create", module: "alumni", entityType: "alumni_profile", entityId: row.id, campusId: row.campusId, after: { name: row.name, directoryVisible: row.directoryVisible } }); revalidatePath("/alumni/profiles"); return { ok: true, data: { id: row.id }, message: "Alumni profile created." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to create alumni profile." }; } }
export async function createAlumniEventAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = alumniEventSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Alumni event details are invalid."); try { const user = await requirePermission("alumni:create"); const row = await createAlumniEvent(user, parsed.data); await writeAuditLog(user, { action: "create", module: "alumni", entityType: "alumni_event", entityId: row.id, campusId: row.campusId, after: { name: row.name, status: row.status } }); revalidatePath("/alumni/events"); return { ok: true, data: { id: row.id }, message: "Alumni event created." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to create alumni event." }; } }
export async function createAlumniEventRegistrationAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = alumniEventRegistrationSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Registration details are invalid."); try { const user = await requirePermission("alumni:create"); const row = await createAlumniEventRegistration(user, parsed.data); await writeAuditLog(user, { action: "create", module: "alumni", entityType: "event_registration", entityId: row.id, campusId: row.campusId, after: { referenceId: row.referenceId } }); revalidatePath("/alumni/events"); return { ok: true, data: { id: row.id }, message: "Attendee registered." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to register attendee." }; } }
export async function createMentorshipAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = mentorshipSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Mentorship details are invalid."); try { const user = await requirePermission("alumni:create"); const row = await createMentorship(user, parsed.data); await writeAuditLog(user, { action: "create", module: "alumni", entityType: "mentorship", entityId: row.id, campusId: row.campusId, after: { status: row.status } }); revalidatePath("/alumni/mentorship"); return { ok: true, data: { id: row.id }, message: "Mentorship request created." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to create mentorship request." }; } }
export async function createJobBoardPostAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = jobBoardPostSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Job post details are invalid."); try { const user = await requirePermission("alumni:create"); const row = await createJobBoardPost(user, parsed.data); await writeAuditLog(user, { action: "create", module: "alumni", entityType: "job_board_post", entityId: row.id, campusId: row.campusId, after: { name: row.name, status: row.status } }); revalidatePath("/alumni/jobs"); return { ok: true, data: { id: row.id }, message: "Job post saved as draft." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to create job post." }; } }
export async function createAlumniDonationAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = alumniDonationSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Donation details are invalid."); try { const user = await requirePermission("alumni:create"); const row = await createAlumniDonation(user, parsed.data); await writeAuditLog(user, { action: "create", module: "alumni", entityType: "alumni_donation", entityId: row.id, campusId: row.campusId, after: { name: row.name, status: row.status } }); revalidatePath("/alumni/donations"); return { ok: true, data: { id: row.id }, message: "Donation recorded in the alumni register." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to record alumni donation." }; } }

export async function createCmsPageAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = cmsPageSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "CMS page details are invalid."); try { const user = await requirePermission("cms:create"); const row = await createCmsPage(user, parsed.data); await writeAuditLog(user, { action: "create", module: "cms", entityType: "cms_page", entityId: row.id, campusId: row.campusId, after: { slug: row.slug, status: row.status } }); revalidatePath("/cms/pages"); return { ok: true, data: { id: row.id }, message: "CMS page created as draft." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to create CMS page." }; } }
export async function transitionCmsPageAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = communityTransitionSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "CMS transition is invalid."); try { const user = await requirePermission("cms:publish"); const row = await transitionCmsPage(user, parsed.data.id, parsed.data.toStatus); await writeAuditLog(user, { action: "update", module: "cms", entityType: "cms_page", entityId: row.id, campusId: row.campusId, after: { status: row.status } }); revalidatePath("/cms/pages"); return { ok: true, data: { id: row.id }, message: `Page moved to ${row.status}.` }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to transition CMS page." }; } }
export async function createCmsMediaAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = cmsMediaSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "CMS media details are invalid."); try { const user = await requirePermission("cms:create"); const row = await createCmsMedia(user, parsed.data); await writeAuditLog(user, { action: "create", module: "cms", entityType: "cms_media", entityId: row.id, campusId: row.campusId, after: { name: row.name, status: row.status } }); revalidatePath("/cms/media"); return { ok: true, data: { id: row.id }, message: "Media registered as draft." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to register media." }; } }
export async function transitionCmsMediaAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = communityTransitionSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Media transition is invalid."); try { const user = await requirePermission("cms:publish"); const row = await transitionCmsMedia(user, parsed.data.id, parsed.data.toStatus); await writeAuditLog(user, { action: "update", module: "cms", entityType: "cms_media", entityId: row.id, campusId: row.campusId, after: { status: row.status } }); revalidatePath("/cms/media"); return { ok: true, data: { id: row.id }, message: `Media moved to ${row.status}.` }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to transition media." }; } }
export async function createCmsFormAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = cmsFormSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Form details are invalid."); try { const user = await requirePermission("cms:create"); const row = await createCmsForm(user, parsed.data); await writeAuditLog(user, { action: "create", module: "cms", entityType: "form", entityId: row.id, campusId: row.campusId, after: { name: row.name, status: row.status } }); revalidatePath("/cms/forms"); return { ok: true, data: { id: row.id }, message: "Form created as draft." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to create form." }; } }
export async function transitionCmsFormAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = communityTransitionSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Form transition is invalid."); try { const user = await requirePermission("cms:publish"); const row = await transitionCmsForm(user, parsed.data.id, parsed.data.toStatus); await writeAuditLog(user, { action: "update", module: "cms", entityType: "form", entityId: row.id, campusId: row.campusId, after: { status: row.status } }); revalidatePath("/cms/forms"); return { ok: true, data: { id: row.id }, message: `Form moved to ${row.status}.` }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to transition form." }; } }
export async function createFormSubmissionAction(input: unknown): Promise<ActionResult<{ id: string }>> { const parsed = formSubmissionSchema.safeParse(input); if (!parsed.success) return invalid(parsed.error.issues[0]?.message ?? "Submission details are invalid."); try { const user = await requirePermission("cms:create"); const row = await createFormSubmission(user, parsed.data); await writeAuditLog(user, { action: "create", module: "cms", entityType: "form_submission", entityId: row.id, campusId: row.campusId, after: { formId: row.formId, status: row.status } }); revalidatePath("/cms/submissions"); return { ok: true, data: { id: row.id }, message: "Form submission recorded." }; } catch (error) { return { ok: false, error: error instanceof Error ? error.message : "Unable to record submission." }; } }
