import { PageHeader } from "@/components/common/page-header";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { listLowAttendance } from "@/lib/api-client/server-queries";
import { requirePermission } from "@/lib/auth/guards";

export default async function AttendanceReportsPage() { const user = await requirePermission("attendance:read"); const rows = await listLowAttendance(user); return <div className="space-y-6"><PageHeader title="Attendance reports" description="Identify active students below the configured 75% attendance threshold from recorded attendance states." /><Card><CardHeader><CardTitle>Low attendance ({rows.length})</CardTitle></CardHeader><CardContent>{rows.length ? <div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left"><th className="p-3">Student</th><th className="p-3 text-right">Recorded</th><th className="p-3 text-right">Attended</th><th className="p-3 text-right">Attendance</th></tr></thead><tbody>{rows.map((row) => <tr className="border-b last:border-0" key={row.studentId}><td className="p-3">{row.student}</td><td className="p-3 text-right">{row.total}</td><td className="p-3 text-right">{row.attended}</td><td className="p-3 text-right">{row.percentage}%</td></tr>)}</tbody></table></div> : <p className="rounded-lg border border-dashed p-8 text-center text-sm text-muted-foreground">No students are below the threshold in the current scope.</p>}</CardContent></Card></div>; }
