import { AccountingWorkspacePage } from "@/features/finance/components/accounting-workspace-server";
export default function ExpensesPage() { return <AccountingWorkspacePage kind="expenses" title="Expenses" description="Record expenses against a scoped chart-of-accounts entry for later approval and reconciliation." />; }
