import { AccountingWorkspacePage } from "@/features/finance/components/accounting-workspace-server";
export default function ReconciliationPage() { return <AccountingWorkspacePage kind="reports" title="Reconciliation" description="Review scoped ledger source rows before matching them with bank statements." />; }
