import { AccountingWorkspacePage } from "@/features/finance/components/accounting-workspace-server";
export default function ChartOfAccountsPage() { return <AccountingWorkspacePage kind="chart" title="Chart of accounts" description="Maintain tenant-scoped account codes used by fee, expense and ledger workflows." />; }
