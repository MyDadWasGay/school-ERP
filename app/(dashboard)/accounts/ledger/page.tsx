import { AccountingWorkspacePage } from "@/features/finance/components/accounting-workspace-server";
export default function LedgerPage() { return <AccountingWorkspacePage kind="ledger" title="Ledger" description="Review immutable finance postings and their source references." />; }
