import { Inbox } from "lucide-react";
import type { ReactNode } from "react";
export function EmptyState({ title = "Nothing here yet", description = "New records will appear here when they are created.", action, filtered = false }: { title?: string; description?: string; action?: ReactNode; filtered?: boolean }) { return <div className="flex flex-col items-center justify-center rounded-lg border border-dashed p-10 text-center"><Inbox aria-hidden="true" className="mb-3 h-8 w-8 text-muted-foreground" /><h3 className="font-medium">{title}</h3><p className="mt-1 max-w-lg text-sm text-muted-foreground">{description}</p>{filtered ? <p className="mt-2 text-xs text-muted-foreground">Try clearing a filter or changing your search.</p> : null}{action ? <div className="mt-4">{action}</div> : null}</div>; }
